pipelines:
  test:
    blubberfile: blubber.yaml
    stages:
      - name: test
  kartotherian:
    blubberfile: blubber.yaml
    stages:
      - name: test
      - name: production
        build: production
        publish:
          image:
            tags: [kartotherian]

